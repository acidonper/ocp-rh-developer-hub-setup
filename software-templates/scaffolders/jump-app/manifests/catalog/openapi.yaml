---
openapi: 3.0.3
info:
  title: Jump App Golang Backend API
  version: 1.0-SNAPSHOT
servers:
- url: https://back-golang-${{ values.ocp_namespace }}.${{ values.ocp_apps_domain }}
  description: Auto generated value
paths:
  /:
    get:
      tags:
      - Jump App Golang
      responses:
        "200":
          description: OK
          content:
            text/plain:
              schema:
                type: string